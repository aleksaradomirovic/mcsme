[build-system]
requires = [ "hatchling >= 1.27" ]
build-backend = "hatchling.build"

[project]
name = "MCSME"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
    "platformdirs>=4.3.0",
    "javaproperties>=0.8.0"
]
